

@{
    ViewData["Title"] = "Home Page";
}
<link type="text/css" rel="stylesheet" href="css/style.css" />
<div class="text-center">
    <h1 class="display-4">這是首頁!!!!!!!</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
    <ul id="ul1">測試</ul>
    <table id="table1" class="table table-striped table-hover">
        <thead>
            <tr>
                <th>姓名</th>
                <th>年資</th>
                <th>薪資</th>
            </tr>
        </thead>
        <tbody>
            <!-- 動態產生 -->
        </tbody>
    </table>
</div>

    <script>
/*         function printSomething(data) {
            const promise = new Promise(function (resolve, reject)
            { setTimeout(function () { resolve(data)
            }, 1000); 
            })
            return promise;
        }
        (async () => {
            let data = await printSomething("A")
            console.log(data)
            data = await printSomething("B")
            console.log(data)
            data = await printSomething("C")
           console.log(data)
        })(); */


/*         emp.employees.forEach(x => {
            var A = x.name;
            var B = x.workYears;
            var C = x.salary;
        console.log(`${A}`+`年資:`+`${B}`+`薪資:`+`${C}`);
        });

        for (i = 0, max = emp.employees.length; ; i++) {
            console.log(emp.employees[i].name)
        console.log(emp.employees[i].workYears)
        console.log(emp.employees[i].workYears)
        } */
    var emp = {
        "employees": [
            { "name": "Tom", "workYears": 3, "salary": 35000 },
            { "name": "Jack", "workYears": 5, "salary": 40000 },
            { "name": "Mary", "workYears": 7, "salary": 45000 }
        ]
    };

    //解構賦值
        const tb = document.querySelector('#table1>tbody');
        const emps = emp.employees.map(x => {
            const { name, workYears, salary } = x;
            return(`<tr>
            <td>${name}</td>
            <td>${workYears}</td>
            <td>${salary}</td>
            </tr>`)
        })
        console.log(emps);
        console.log(emps);

        tb.innerHTML = emps.join("");



        // const x = emp.employees.map(emp =>`<li>姓名:${emp.name}</li>`);
        // console.log(x);
        // const y = x.join("");
        // console.log(y);
        // document.getElementById(`ul1`).innerHTML += y;






    </script>

